# Query Drafts

Legacy query draft area.

Generated, test, validation, lab-specific, host-specific, and investigation-specific queries are temporary by default and should not be added to GitHub. Store a query in `queries_and_scripts/custom_queries/` only when the user explicitly asks to save that exact query as a custom query.
