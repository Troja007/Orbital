# Working Files

Legacy working area for drafts, adapted templates, experiments, and files that are actively being changed here.

Do not overwrite source files directly. Copy or reference catalog source material from `queries_and_scripts/` in a temporary/local working area before modifying it.

Generated SQL/JSON query artifacts are temporary by default. Do not commit new generated queries from this area unless the user explicitly asks to store a specific query as a custom query under `queries_and_scripts/custom_queries/`.

Key working areas:

- Product and workflow context now lives at root-level `../product-context/`.
