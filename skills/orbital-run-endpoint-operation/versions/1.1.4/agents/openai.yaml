interface:
  display_name: "Orbital Run osquery Live Query"
  short_description: "Run targeted Orbital osquery queries safely."
  default_prompt: "Use the configured Orbital API to run a targeted osquery query, choosing scheduled or live mode safely, and return the requested result summary."
